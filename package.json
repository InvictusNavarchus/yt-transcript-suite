{
	"name": "youtube-transcript-monorepo",
	"private": true,
	"type": "module",
	"workspaces": [
		"packages/*"
	],
	"catalog": {
		"typescript": "^7.0.1-rc",
		"@types/bun": "^1.3.14",
		"@types/node": "^26.0.1"
	},
	"scripts": {
		"cli": "bun --filter '@youtube-transcript/cli' run cli",
		"inspect": "bun --filter '@youtube-transcript/cli' run inspect",
		"dev:server": "bun --filter '@youtube-transcript/server' run dev",
		"dev:userscript": "bun --filter '@youtube-transcript/userscript' run dev",
		"build:scraper": "bun --filter '@youtube-transcript/scraper' run build",
		"build:userscript": "bun --filter '@youtube-transcript/userscript' run build",
		"typecheck": "bun --filter '*' run typecheck",
		"test": "bun --filter '@youtube-transcript/scraper' run test",
		"format": "bunx @biomejs/biome format --write .",
		"lint": "bunx @biomejs/biome lint .",
		"prepare": "lefthook install"
	},
	"devDependencies": {
		"@biomejs/biome": "2.5.1",
		"@types/node": "catalog:",
		"lefthook": "^2.1.9",
		"typescript": "catalog:"
	}
}
