# The port the local transcript server will listen on
PORT=3456

# Optional API key for authenticating requests. 
# If set, the userscript must send this exact key in the x-api-key header.
# If left blank, request authentication will be bypassed (trusted local environment).
SERVER_API_KEY=your_secure_api_key_here
